# Base image
FROM node:16

# Set working directory
WORKDIR /app

# Copy package.json and package-lock.json
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy all files (e.g., server.js and public/)
COPY . .

# Expose the port
EXPOSE 3000

# Start the server
CMD ["node", "server.js"]
